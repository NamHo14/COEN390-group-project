# Firmware Tests

This folder is reserved for host-side or embedded tests once a firmware build system is selected.

Planned starter tests:
- Rep detector state transition checks.
- BLE command parsing checks.
- Exercise routing checks.
